# NPM Configuration for Natural Clinic Frontend
# This ensures consistent dependency resolution across environments

# Use legacy peer deps to avoid version conflicts
legacy-peer-deps=true

# Engine strict mode off to prevent node version errors
engine-strict=false

# Save exact versions
save-exact=false

